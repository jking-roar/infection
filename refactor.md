The views of Collection, Create, Combine, and Infect are all very similar.
Rework the Collection view to be more of a "Lab" where the user can see all their viruses and perform actions on them. The view will have the following features:
- Call it the Lab rather than Collection
- click a virus to show commands for that virus
  - view details
  - share
    - via text
    - via QR code
  - clean - name this something relevant to the funfection app
  - combine
    - this virus will be a "left side" of combinations and the user can select "right side" virus(es) in a dialog
- Add a "Create" virus on the bottom
  - dialog will ask for the inspriration (seed) and after creation the app will show the details of the new virus.

Details view will have similar commands as the collection view (sans View) and a "Back to Lab" button